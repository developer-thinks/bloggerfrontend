export default (posts = [],action)=>{
    switch (action.type) {
        case "A":
            return posts;
            break;
    
        default:
            return posts;
            break;
    }
}
